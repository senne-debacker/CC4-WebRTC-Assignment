# CC4-WebRTC-Assignment

## Concept: Simon Says

**Description**
A digital version of the famous game 'Simon Says'. The user connects to the laptop by scanning a QR-code and starts the game. The laptop will show commands on the screen. If the command is preceded by the text 'Simon Says', the user has to execute the command, of not, they cannot execute the command. If they fail before the timer ends, it is game over. 

**Commands**

Using screen
- Tap the screen
- Draw something on the screen
- (AI) Smile
- ...

Using gyroscope/accelerometer
- Shake
- Jump
- Duck
- Do a 360
- Pour me a drink
- ...

Using microphone
- Yell/talk
- ...

Using camera
- ...

## Planning

**Monday:** test phone data like accelerometer and gyroscope
**Tuesday:** start on basic html & think of more commands
**Thursday:** add logic to show commands

## DevDiary

### AI Usage Categories
1. **Minimal**: Little to no assistance, pure custom logic.
2. **AI Consult**: Minimal help, only chat-based questions (no AI generated code).
3. **Copilot**: Significant use of Copilot for troubleshooting or boilerplate.
4. **AI Generated**: Code primarily generated by AI.

### Day: 25-02-2026

#### 1. Github & Project setup
| Section | Details |
| :--- | :--- |
| **Description** | Initialized the local Git repository, created the remote on GitHub, and established the project folder structure. |
| **AI Usage** | **Minimal** |

##### Question 01
**Input**
/

**Return**
/

#### 2. Followed tutorial: webrtc_02-webcam-basic
| Section | Details |
| :--- | :--- |
| **Description** | Implemented a basic webcam feedback on a local server. |
| **AI Usage** | **Minimal** |

##### Question 01
**Input**
/

**Return**
/

#### 3. Followed tutorial: webrtc_03-webcam-local-https
| Section | Details |
| :--- | :--- |
| **Description** | Implemented a local HTTPS environment to test webcam access, following the WebRTC_03 tutorial steps. |
| **AI Usage** | **Minimal** |

##### Question 01
**Input**
/

**Return**
/

#### 4. Followed tutorial: webrtc_05-streaming-clients-list
| Section | Details |
| :--- | :--- |
| **Description** | Continued following the tutorials on WebRTC. |
| **AI Usage** | **Minimal** |

##### Question 01
**Input**
/

**Return**
/

#### 5. Finished Prototype
| Section | Details |
| :--- | :--- |
| **Description** | Finished tutorial using Claude Opus, we now have a working prototype for a peer 2 peer connection where a webcam feedback is shared. |
| **AI Usage** | **AI Consult** |

##### Question 01
**Input**
The video canvas is rendering, and the camera is being used, but i'm not seeing the feedback on the canvas, DONT fix the code, tell me how to do it

**Return**
I'm creating a short checklist of diagnostic and fix steps for your video issue.

Progress: checklist added; next I'll explain the exact fixes to apply.

What to do (step-by-step, do not edit for me):

Fix the API typo: replace navigator.mediaDevice.getUserMedia with navigator.mediaDevices.getUserMedia(...) — the property is plural.
Fix the assignment typo: replace $video.scrObject = stream with $video.srcObject = stream.
Allow autoplay: add attributes to the <video> element: <video id="myVideo" autoplay playsinline muted></video>. Many browsers block autoplay unless muted or initiated by a user gesture.
Call play explicitly: after assigning the stream do await $video.play() (or $video.play().catch(console.error)).
Ensure secure context: open the page via http://localhost or https (getUserMedia is blocked on  or insecure origins). Quick test command:
Run a simple server in the project root:
then open http://localhost:8080 (or the port printed).
Verify in DevTools:
Open Console (Cmd+Opt+J) and confirm no errors.
Inspect the video element in the Elements panel and check that srcObject is a MediaStream.
If play() is rejected, the console will show the reason (autoplay policy or permission).
Visual checks:
Make sure the <video> has non-zero size or CSS that makes it visible (e.g., add width/height or style="width:640px;height:480px").
Ensure no CSS display:none or overlay covering it.
Likely cause in your file (index.html): the runtime error comes from navigator.mediaDevice being undefined (typo) so getUserMedia was never called; additionally scrObject prevents the stream from attaching even if obtained.

If you want, I can apply these exact small edits to index.html and test locally — say the word "apply" and I'll make the changes and run a quick server.

##### Question 02
**Input**

What is going wrong here? This page isn’t working
localhost didn’t send any data.
ERR_EMPTY_RESPONSE

Just explain, don't generate code

**Return**

Diagnosis

No server listening: Your Node/Express process may not be running, so localhost closed the connection immediately.
Server crashed on start: The server may have thrown an error and exited (check the terminal where you started it).
Wrong port or host: The page is hitting a port where nothing is listening, or the server is bound to a different interface.
Protocol mismatch (HTTP/HTTPS): If you created localhost.crt/localhost.key and started an HTTPS server, but you open http:// (or vice‑versa), the connection can fail with no response.
Server accepted connection but never responded: Your handler might hang or never send a response (e.g., missing final response/end), causing the client to see an empty reply.
Firewall / proxy / socket conflict: A local firewall, VPN, or another process can interrupt the connection.
Permission / file access error: If the server needs the cert/key or other files and can’t read them, it might fail silently at startup.
Suggested quick checks (no code):

Look at the terminal where you launched the server for errors or stack traces.
Confirm the server process is running and which port it reports listening on.
Verify whether you intended HTTP or HTTPS and that you’re opening the matching URL.
Check server logs to see if requests are received and whether a response is written.
Inspect the browser Network tab to see request details and whether the TCP connection opens then closes.
If you tell me what you see in the server terminal or which URL/protocol you’re loading, I’ll pinpoint the most likely cause.

##### Question 03
**Input**

What is missing here?

s_backer@Sennes-MacBook-Air CC4-WebRTC-Assignment % npm start

cc4-webrtc-assignment@1.0.0 start
node index.js

/Users/s_backer/Documents/school/25-26/sem2/cc4/assignments/CC4-WebRTC-Assignment/index.js:23
server.listen(port, () => {
^

TypeError: Cannot read properties of undefined (reading 'listen')
at Object.<anonymous> (/Users/s_backer/Documents/school/25-26/sem2/cc4/assignments/CC4-WebRTC-Assignment/index.js:23:8)
at Module._compile (node:internal/modules/cjs/loader:1546:14)
at Object..js (node:internal/modules/cjs/loader:1698:10)
at Module.load (node:internal/modules/cjs/loader:1303:32)
at Function._load (node:internal/modules/cjs/loader:1117:12)
at TracingChannel.traceSync (node:diagnostics_channel:322:14)
at wrapModuleLoad (node:internal/modules/cjs/loader:218:24)
at Function.executeUserEntryPoint [as runMain] (node:internal/modules/run_main:170:5)
at node:internal/main/run_main_module:36:49

Node.js v23.3.0

**Return**

I'll open index.js to inspect how server is (not) defined.

Short answer: server is undefined because you shadowed the outer variables inside the if/else blocks.

Details:

In index.js:1-24 you declare let options = {}; let server; at top, then inside the if and else you use const options = … and const server = …. Those const declarations create new block-scoped variables that shadow the outer ones, so the outer server remains undefined when you call server.listen(...).
The same shadowing hides the populated options object, so your HTTPS setup never assigns the cert/key to the top-level options.
What to do (no code):

Remove the inner const/let so the assignments write to the outer options and server variables (or assign to the outer variables explicitly).
After that, confirm the server actually binds and logs the listening message.
Extra: running on port 443 requires elevated privileges on macOS (ports <1024); if you get EACCES later, either run with proper privileges or use a non-privileged port for development.
That will resolve the TypeError and let server.listen run.


### Day: 26-02-2026

#### 1. Requirements Check
| Section | Details |
| :--- | :--- |
| **Description** | Used AI to scan the code and check for the requirements. Result: The requirement “Websockets are only used for the signalling layer” is met, but the requirement “uses WebRTC data channels for the controls” is not — controls are currently not implemented over data channels. |
| **AI Usage** | **AI Consult** |

##### Question 01
**Input**
Functional Requirements
Minimum:

Needs a one-to-one setup using a QR code
You may repurpose an old project you made yourself.
Make sure the instructions are clear. Keep it simple.
It uses webrtc data channels for the controls. Websockets are only used for the signalling layer.
Your project is up and running by executing "npm install" and "npm start". This launches a local webserver, where your html / css / javascript frontend is hosted. You do not need to host it online.
You use Git + Github to version control your project. Work towards a working MVP every week and use a GitHub Flow workflow.
README.md file: Markdown file where you document your step-by-step development process + a critical reflection of your use of AI: how did you use AI, what code did AI write for you and what parts of the AI generated code did you modify (code green, see below) in your process.
Bonus points:

accelerometer / gyroscope / other smartphone sensor input
Video / Audio channel integration as an integrated part of the experience (aka: the video / sound needs te be useful).

Look at these requirements, is this in accordance with our use of websockets?

**Return**
Progress: I scanned the repo files and verified how sockets are used.

Server: index.js — Socket.IO only implements signaling: it tracks clients and forwards signal events plus emits clients, client-connection, client-disconnect.
Client: index.html — connects to Socket.IO, sends/receives signal messages for SimplePeer. WebRTC is used for media (stream) via SimplePeer, but there is no peer.on('data') or peer.send(...) anywhere — controls are not sent over a data channel.
Compliance: Not fully. The requirement “Websockets are only used for the signalling layer” is met, but the requirement “uses WebRTC data channels for the controls” is not — controls are currently not implemented over data channels.
Fix (recommended): Add data-channel handlers in createPeer:
listen: peer.on('data', data => handleControl(JSON.parse(data)))
send controls: peer.send(JSON.stringify({type:'control', payload: {...}}))
This moves control messages off Socket.IO and onto the WebRTC data channel.
Want me to implement the data-channel send/receive in index.html now?

#### 2. Phone Gyroscope Data
| Section | Details |
| :--- | :--- |
| **Description** | Added a function where the phone gyroscope data is requested and then sent to the laptop. |
| **AI Usage** | **AI Generated** |

##### Question 01
**Input**
Can I add accelerometer data?

**Return**
I'll add a short todo plan, then patch index.html to stream accelerometer data over the WebRTC data channel and display it in the UI.

Patching; will add accelerometer listener and data-channel handlers to index.html.

Now I'll patch index.html to request motion permissions and send throttled accelerometer (devicemotion) events over the existing WebRTC data channel, plus show received accel data in the UI.

Done — accelerometer (devicemotion) support added.

What changed: index.html now requests motion permissions, throttles devicemotion events, sends accelerometer data over the existing WebRTC data channel, and displays remote/local accel values in the "Phone Data" panel.
Next: test locally with two devices (laptop + phone). If you want, I can:
add visual mapping from accel to game controls, or
reduce/increase throttle, or
add a toggle to enable/disable sending.
Which of those should I do next?

### Day: 04-03-2026

#### 1. 
| Section | Details |
| :--- | :--- |
| **Description** | Initialized the local Git repository, created the remote on GitHub, and established the project folder structure. |
| **AI Usage** | **Minimal** |

##### Question 01
**Input**
/

**Return**
/